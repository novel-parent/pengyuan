<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pengyuan.backstage.mapper.FactoryMapper">
	
	<select id="selectFactoryByFid" resultType="com.pengyuan.backstage.bean.Factory">
		select * from factory where fid = #{fid}
	</select>
</mapper>